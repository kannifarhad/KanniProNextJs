export enum LoadingEnum {
  IDLE = "idle",
  LOADING = "loading",
  SUCCESS = "success",
  FAILED = "failed",
}
export enum LangOptionsEnum {
  EN = "en",
  AZ = "az",
}

export const DefaultLang = LangOptionsEnum.EN;

export type ColorsEnum = "green" | "primary";
